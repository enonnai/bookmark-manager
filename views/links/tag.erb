<h1> Links tagged with <%= @tag.tag %> </h1>

<ul id='links'>
  <% @tag_links.each do |link| %>
  <li>
    Title: <%= link.title %>
    URL:   <%= link.url   %>
    Tags: <%= link.tags.map(&:tag) %>
  </li>
  <% end %>
</ul>
